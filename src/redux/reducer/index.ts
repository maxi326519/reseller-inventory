/* import {

} from "../actions/index";
 */
const initialState = {

};

export const Reducer = (state:any = initialState, action:any) => {
  switch (action.type) {
    default:
      return state;
  }
};
